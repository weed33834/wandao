# Claude 改进实施记录（2026-07-27）

本记录整理 Claude 在 `d25e370` 基线上完成的审查与改进成果，并记录维护者接收时的复核结论。完整代码审查见同目录的《万能导审查报告-20260726》，各批次实施细节见 `claude-batches/`。

## 已接收范围

本次只接收低风险与中风险批次，并保留原始 Git 提交和 `sol/*` 标签，方便逐项追踪或回退。

- Python 子进程与结构化日志统一 UTF-8 输出，减少 Windows 中文乱码。
- 文件名规避 `CON`、`PRN`、`AUX`、`NUL`、`COM1`、`LPT1` 等 Windows 保留设备名。
- checkpoint 租约允许原运行在无人接管时恢复续期，避免慢任务或系统睡眠后被错误终止。
- CDP socket 超时和连接中断统一包装为可诊断的 `ExportError`。
- 插件包路径拦截 NTFS ADS、Windows 非法字符、保留名以及结尾点和空格。
- Plugin v1 对未来新增的未知顶层字段采用 tolerant reader：告警并忽略；必填字段、权限、入口路径、版本、平台和签名完整性仍严格校验。
- `parseLastJson` 避免对缩进 JSON 片段反复解析，消除大报告下的 O(n²) 主进程阻塞。
- OneNote C# 桥接统一 UTF-8 输出，并发清空 stderr，避免中文乱码和管道写满死锁。
- 错误分类补充网络连接、网络超时、DNS、证书/代理和远端 404，并减少本地路径、API 权限等误判。
- 完整发现 `tests_js/*.test.js`，新增测试无需再手工加入主清单；缺少真实 Electron 二进制时只跳过明确依赖它的测试。
- 首屏、平台卡片、插件中心与任务结果区进行空间压缩、长文本保护和无障碍对比度修正。
- 增加统一行尾策略、常见本地构建目录忽略规则和更清晰的 Windows 安装包名称。

## 未接收的高风险批次

以下代码继续保留在独立分支，不进入本次稳定整合：

- `claude/b3-sec`：`open-path` 分层路径边界与 `write-file` 白名单。
- `claude/b3-hist`：任务历史瘦身与 DPAPI 加密缓存。
- `claude/b3-path`：路径长度收敛与飞书单篇失败降级。

这些改动会改变运行时安全边界、历史数据格式或真实平台导出行为，需要 Windows 存量数据和真实账号人工验收后再单独处理。

## 接收时调整

- 删除 `.github/FUNDING.yml`：文件只有爱发电占位地址 `REPLACE_WITH_YOUR_ID`，不能发布到仓库。
- `.improve/` 只作为本地 bundle、patch 和临时报告交付目录，加入 `.gitignore`；有长期价值的报告已归档到 `docs/reviews/`。
- `.worktrees/` 继续保持忽略，避免本地工作树副本进入 Git。

## 验证结果

在隔离工作树 `codex/review-claude-improvements` 上重新执行：

- `python scripts/quality_check.py`：通过。
- Python：547 项通过，3 项按环境条件跳过。
- Node.js：183 项通过。
- Plugin/Provider 校验：14 个插件、20 个 Provider 通过。
- Git 空白检查：通过。
- 视觉冒烟：首页、平台中心、插件中心、夜间模式均未发现明显遮挡、溢出或对比度回归。

接收过程中同时移除了 WPS JavaScript 正则字符串和息流测试样例中的两个 Python 非法转义警告，并删除目录级 Node 测试发现所不再需要的临时自注册清单。

## 后续人工验收

- Windows OneNote：中文标题、大批量页面、桥接产生大量 stderr 时的稳定性。
- 安装包：新文件名、覆盖升级、快捷方式名与卸载列表显示。
- 高风险三个分支：按各自 `claude-batches/batch3-*.md` 清单验证后单独合入，避免与稳定改进捆绑。
