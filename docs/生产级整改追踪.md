# 生产级整改追踪

本清单以“可验证关闭”为准：实现、回归测试、质量门禁和用户可观察行为均满足后才可标记完成。状态中的“进行中”表示当前工作分支已有专职修复，不代表可发布。

## P0：数据、凭证与可恢复性

| ID | 项目 | 状态 | 完成标准 |
| --- | --- | --- | --- |
| REL-001 | checkpoint 并发租约 | 进行中 | 活跃任务不可被第二进程恢复；租约过期才可接管；关闭释放租约。 |
| REL-002 | 增量导出完整性 | 待办 | 只有 checkpoint 已完成且 Markdown/资源完整时才跳过；资源失败项必须重试。 |
| REL-003 | 导入幂等性 | 待办 | 语雀、飞书、印象、ima 在远端创建后崩溃时可复用远端 ID，不重复创建。 |
| SEC-001 | 语雀/飞书 Cookie 重定向边界 | 进行中 | 禁止自动跨域复制 Cookie；每跳 HTTPS、域与 Cookie 规则均校验。 |
| SEC-002 | 有道 Cookie 与资源 URL 边界 | 已完成 | 仅受信任 HTTPS 有道域可带 Cookie；跨域跳转重新建无 Cookie 请求。 |
| SEC-003 | 导入本地路径边界 | 部分完成 | 语雀/飞书/印象/ima 已限制；其余文件型导入器持续接入共享解析器。 |
| SEC-004 | 统一受限下载器 | 待办 | 所有远程资源下载拒绝私网/IP、HTTP、危险重定向与超大响应，并原子落盘。 |

## UX：优先完成

| ID | 项目 | 状态 | 完成标准 |
| --- | --- | --- | --- |
| UX-001 | 结果状态真实性 | 已完成 | 完成、部分完成、失败、停止的历史 badge 与即时进度一致。 |
| UX-002 | 停止与导航保护 | 已完成 | 停止请求显示 stopping；运行中不能丢失表单上下文。 |
| UX-003 | 任务结束结果卡 | 进行中 | 当前页面显示统计、失败摘要、报告/输出/复制/恢复 CTA。 |
| UX-004 | 长页面任务可见性 | 进行中 | 进度和结果卡在长页面保持可见且可访问。 |
| UX-005 | 表单草稿与字段错误 | 待办 | 切换、重启、验证错误后保留用户输入并定位字段。 |
| UX-006 | 大目录树与任务中心 | 待办 | 大列表虚拟化/分批渲染、筛选、状态/时间/平台过滤。 |
| UX-007 | 可访问性与键盘操作 | 待办 | 焦点管理、动态状态播报、对比度和键盘完整操作。 |

## 平台与架构

| ID | 项目 | 状态 | 完成标准 |
| --- | --- | --- | --- |
| ARC-001 | Tauri/renderer 模块拆分 | 待办 | 任务编排、页面渲染、历史、Provider 适配可独立测试。 |
| ARC-002 | 复杂导入步骤流 | 待办 | 飞书/语雀导入在 UI 提供前置检查、确认、执行、恢复的可见步骤。 |
| ARC-003 | Provider 重试语义 | 待办 | 文档失败与资源失败分别统计，CTA 不承诺平台不支持的精确重试。 |
| ARC-004 | 旧 GUI/兼容入口治理 | 待办 | 保留必要兼容入口，移除不可测试的旧 GUI 主流程。 |

## 质量、发布与运维

| ID | 项目 | 状态 | 完成标准 |
| --- | --- | --- | --- |
| OPS-001 | 质量门禁覆盖任务结果 | 已完成 | 共享任务报告测试已接入 quality_check。 |
| OPS-002 | 发行资产可验证性 | 部分完成 | Release 附带 SHA256SUMS 与 SPDX SBOM；provenance 通过 GitHub attestation。 |
| OPS-003 | Tauri 2 GUI E2E | 待办 | Windows/macOS 上覆盖启动、Provider 发现、执行/停止/恢复的黑盒流程。 |
| OPS-004 | 依赖、Secret 与 CodeQL 审计 | 已完成 | CI 已配置 npm/pip 审计、gitleaks、CodeQL；需定期处理告警。 |
| OPS-005 | 桌面签名与公证增强 | 可选 | 当前按项目发布策略提供未签名安装包；未来采购证书后可增加 Windows/macOS 签名、公证和密钥轮换。 |
| OPS-006 | 备份与支持包 | 待办 | 脱敏诊断包、任务/配置备份与恢复流程可验证。 |

## 验收顺序

1. 合并并验证 UX-003/004、REL-001、SEC-001。
2. 以语雀、飞书为样板关闭 REL-002/003、SEC-004，再迁移其余资源型 Provider。
3. 完成表单草稿、任务中心和导入步骤流。
4. 完成架构拆分与 Tauri 2 GUI E2E。
5. 完成未签名发行风险说明、回滚演练和正式发布验收；桌面签名、公证作为可选增强。
